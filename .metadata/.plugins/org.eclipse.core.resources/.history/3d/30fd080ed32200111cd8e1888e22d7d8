
#include <stdint.h>
#include "stm32f446xx.h"

void SysClockCongig(void){
	RCC->CR |= RCC_CR_HSEONJB;// Enable HSE and wait for the HSE to become ready
	// Set the power enable clock and voltage regulator
	// Configure the Flash PREFETCH and the LATENCY related setting
	// Configure the PRESCALARS HCLK, PCLK1, PCLK2
	// Configure the main PLL
	// Enable the PLL and and wait for it become ready
	// Select the clock source and wait for it to be set
}

int main(void)
{

}
